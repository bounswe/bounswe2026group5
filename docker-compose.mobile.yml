services:
  mobile-test:
    build:
      context: ./mobile
      dockerfile: Dockerfile.dev
    container_name: mentorship_mobile_test
    volumes:
      - ./mobile:/app
      - mobile_node_modules:/app/node_modules
      - /app/coverage
    command: npm test

volumes:
  mobile_node_modules:
