services:
  mobile-test:
    build:
      context: ./mobile
      dockerfile: Dockerfile.dev
    container_name: mentorship_mobile_test
    volumes:
      - ./mobile:/app
      - /app/node_modules
    command: npm test
